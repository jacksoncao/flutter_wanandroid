import 'package:flutter/material.dart';

//收藏网页tab页面
class CollectionWapPage extends StatelessWidget {
  const CollectionWapPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("收藏网址列表"),
    );
  }
}